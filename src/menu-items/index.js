// project import
import pages from './pages';
import navigation from './navigation';
import utilities from './utilities';
import support from './support';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [navigation, pages, utilities, support]
};

export default menuItems;
